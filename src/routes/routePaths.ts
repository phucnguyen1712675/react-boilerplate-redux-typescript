enum ROUTE_PATHS {
  ROOT = '/',
  LOGIN = '/login',
  HOME = '/home',
  POST = '/posts/:postId',
  EDIT_POST = '/editPost/:postId',
  USERS = '/users',
  USER = '/users/:userId',
}

export default ROUTE_PATHS;
